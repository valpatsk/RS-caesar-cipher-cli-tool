class Caesar {
    encode(text,shift){
        //console.log('encode: '+text+', shift:'+shift);
        return text;
    }
    decode(text,shift){
        //console.log('decode: '+text+', shift:'+shift);
        return text;
    }
}


module.exports = Caesar;